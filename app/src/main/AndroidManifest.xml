<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="projekt.auto.launcher.overlay.mbux_gaet"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="projekt.auto.launcher.OVERLAY_COMPONENT" />

    <overlay
        android:priority="1"
        android:targetName="projekt.auto"
        android:targetPackage="projekt.auto" />

    <application
        android:allowBackup="false"
        android:hasCode="false"
        android:label="flowUX synesthesia - MBUX overlay by @Gaet">

        <meta-data
            android:name="overlay_creator"
            android:value="Gaet" />
        <meta-data
            android:name="overlay_title"
            android:value="flowUX synesthesia - MBUX overlay" />
        <meta-data
            android:name="overlay_description"
            android:value="An MBUX designed overlay for the flowUX launcher" />

    </application>

</manifest>